version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu
hooks:
  Install:
    - location: scripts/deploy_webapp.sh
      timeout: 300
      runas: root      
  # ApplicationStart:
  #   - location: deploy/application_start.sh
  #     timeout: 300
  #     runas: root      